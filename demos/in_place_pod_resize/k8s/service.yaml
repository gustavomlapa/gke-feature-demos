apiVersion: v1
kind: Service
metadata:
  name: java-startup-resize-service
  labels:
    app: java-startup-resize-demo
spec:
  type: ClusterIP
  selector:
    app: java-startup-resize-demo
  ports:
  - port: 8080
    targetPort: 8080
    name: http
